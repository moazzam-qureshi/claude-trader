```
Resume Phase 3 strategy pivot of trading-mcp-sandwich. Wave 0 (Foundation)
COMPLETE. Branch phase-3-strategy-pivot has 37 commits, all unpushed.
Memory project_strategy_pivot.md has full state. Detailed handoff at
docs/superpowers/HANDOFF_2026-05-11_CHECKPOINT_1.md.

Working directory: D:/Personal/Projects/trading-mcp-sandwich

Pre-flight:
1. git status — clean tree on phase-3-strategy-pivot. Untracked .claude/
   + scripts/probe_*.sh + scripts/inspect_claude_config.py +
   scripts/add_binance_mcp.sh are pre-existing, leave alone.
2. git log -1 — should be 1823ca1 "docs: next-session prompt for Wave 1
   start". If different, STOP and ask.
3. docker compose ps — bring up if down (ask first).
4. docker compose run --rm test \
     tests/integration/test_settings_repo_set.py \
     tests/integration/test_foundation_smoke.py -q
   MUST be 10 passing. settings_repo_set is the safety net; foundation
   _smoke is the Wave 0 contract. Either failing means broken before
   Wave 1 starts.
5. docker compose run --rm tools alembic current — head 0017.

Read before coding:
- docs/superpowers/HANDOFF_2026-05-11_CHECKPOINT_1.md (full handoff)
- docs/superpowers/specs/2026-05-09-phase-3-strategy-pivot-design.md
  §2.1 (A1 Standard Grid) and §3.5 (deploy_grid sugar)
- docs/superpowers/plans/2026-05-09-phase-3-strategy-pivot.md (Wave 1
  task list 2.1–2.31)
- src/trading_sandwich/strategies/base.py (Strategy ABC contract)
- tests/integration/test_foundation_smoke.py (end-to-end shape)

Pick up at Wave 1 Task 2.1: A1 Standard Grid in strategies/grid/standard.py.

Strategy task pattern (every Wave 1 strategy 2.1–2.25):
  RED unit + integration → GREEN minimal impl + register in
  strategies/worker.py _default_registry → DRY-RUN verify → live
  capability OFF by default → backtest (once 2.26 ships) → ONE COMMIT

Discipline (non-negotiable):
- Docker-only. ONE TASK = ONE COMMIT. TDD: RED → GREEN → commit.
- Conventional Commits. Never skip hooks. Never destructive git.
- Never weaken tests/integration/test_settings_repo_set.py (9 safety
  tests pinning Claude can't raise own circuit breakers).
- Discord /settings (Tier 3) vs /safety (Tier 2 operator-only) split is
  STRUCTURAL — different commands, different authorities. Don't collapse.
- Cold-start regime classification does NOT pivot — first 2-run is
  baseline. Pin this; don't "fix" without spec amendment.
- Halal-spot only. OrderIntent.side is Literal['long']. Tier 1 keys
  inviolable. NEVER propose loosening.

HARD GATE: Task 2.30 (first live deployment) requires EXPLICIT operator
go-ahead even if all tests pass. Real $30 capital on real Binance.
Don't ship live without confirmation.

Pre-existing surfaces (don't re-flag):
- "cli" docker service entrypoint broken — use "tools" or "test".
- runtime/CLAUDE.md + runtime/GOALS.md still reference Phase 2.7
  watchlist + heartbeat trader. Get full rewrites at Tasks 2.27 + 2.28.
- 5 pre-existing baseline test failures in test_invocation.py (3) +
  test_policy_phase2.py (2). Stash-verified unrelated. Ignore.
- strategies/worker.py _default_registry empty — Wave 1 strategies
  populate as they land.

Ask before coding (one batch):
1. Bring docker compose up if down? (yes/wait)
2. Push 37 commits to origin? (last preference: keep local; CHECKPOINT 1
   reached so push is the natural backup point now)
3. Begin Task 2.1 (A1 Standard Grid) immediately, or different ordering?

Then proceed in plan order. STOP at Task 2.30 for live-deployment approval.
```
