# Handoff Prompt — 2026-05-10 night → next session

Copy-paste the block below into the next Claude Code session. It is
self-contained and tells the next agent exactly where to pick up.

---

```
Resume Phase 3 strategy pivot of trading-mcp-sandwich. Branch
phase-3-strategy-pivot has 15 commits, all unpushed. Memory file
project_strategy_pivot.md should auto-load and has full state.

Working directory: D:/Personal/Projects/trading-mcp-sandwich

Pre-flight (do in order, surface failures rather than auto-fixing):

1. git status — confirm clean tree on branch phase-3-strategy-pivot.
   Untracked .claude/ + scripts/probe_*.sh + scripts/inspect_claude_config.py
   + scripts/add_binance_mcp.sh are PRE-EXISTING, not Phase 3 work, leave alone.
2. git log --oneline -16 — last commit should be e251937 "feat(settings):
   three-tier repo set path — SAFETY CRITICAL". If different, STOP and ask me.
3. docker compose ps — if not running, ask before bringing up.
4. Once infra is up, run the safety-critical regression suite:
     docker compose run --rm test tests/integration/test_settings_repo_set.py -q
   MUST be 9 passed. If any fail, STOP — that suite pins the authority
   enforcement that prevents Claude raising its own circuit breakers.
5. docker compose run --rm tools alembic current — head should be 0017.
6. Read in order before any code:
   - docs/superpowers/specs/2026-05-10-db-backed-config-amendment.md (the v2
     amendment, three-tier mutability — read in full)
   - docs/superpowers/specs/2026-05-09-phase-3-strategy-pivot-design.md (full
     spec; v2 amendment supersedes §1.4 row "git-versioned prompts" and arch
     rule "no DB-mirrored prompt table" but only for this instance)
   - docs/superpowers/plans/2026-05-09-phase-3-strategy-pivot.md (62-task plan)
   - src/trading_sandwich/settings/{keys,_halal,_safety_seed,repo}.py (built)

Pick up at TASK AM-4e (settings seed bootstrap).

CRITICAL CONTEXT — what's done vs left:

  DONE (this session):
    - Migrations 0013–0017 (strategies + regime + portfolio_decisions +
      policy_settings + policy_changes + policy_snapshot column)
    - Settings package: keys.py + _halal.py + _safety_seed.py +
      repo.py (get + set with three-tier authority enforcement)
    - 9 SAFETY CRITICAL integration tests pin authority logic
    - Discretionary trader gated (trading_enabled: false) — workers running
      for analytics, no new orders accepted at execution rail
    - Capital is ~$183 USDT (operator topped up +$88 mid-session)
    - 15 commits on branch, NOT pushed to origin

  LEFT in amendment (~5 tasks before resuming Wave 0):
    - AM-4e: src/trading_sandwich/settings/seed.py — bootstrap policy_settings
      from policy.yaml on first run. Idempotent. Tier 1 keys excluded (file-only).
      ~50 lines + tests.
    - AM-4f: src/trading_sandwich/settings/snapshot.py — snapshot_policy()
      returns {settings: <full Tier 2/3 dict>, inviolable: <Tier 1 dict>,
      snapshot_at, git_head}. Embedded into claude_decisions.policy_snapshot
      and portfolio_decisions.policy_snapshot at decision time. ~30 lines.
    - AM-5: 4 MCP tools — get_setting, set_setting (always passes
      authority='mcp_default'), list_settings, get_setting_history. Discord
      notification on every successful set + on operator_only_key rejections.
    - AM-6: Discord slash commands. Two surfaces:
        /settings list/get/set/diff/revert — Tier 3 only, mcp_default authority
        /safety list/set/reset — Tier 2 only. SAFETY CRITICAL: handler MUST
          verify Discord user ID == configured operator ID before passing
          authority='operator_safety'. Non-operator → not_operator audit row.
    - AM-7: cli settings bootstrap/reseed/list/get/diff. Then carefully
      migrate Phase 2.7 callers from _policy.load_policy() to
      settings.repo.get(). Don't break running triage-worker. is_trading_enabled
      stays on Tier 2 file path (works as-is); new Phase 3 code uses repo.

  LEFT in Phase 3 plan (Wave 0 not started):
    - Tasks 1.5 → 1.16 (universe expansion, Strategy ABC, regime classifier,
      performance tracker, MCP read+command tools, Discord slash framework +
      buttons + modals, strategy-worker Celery service, smoke test)
    - Then Wave 1 (25 strategies + backtest + persona swap), Wave 2 (CFGI + 7
      strategies, after 30d Wave 1 stabilization), Wave 3 (on-chain + 5
      strategies, after 30d Wave 2 stabilization)
    - Total: ~62 tasks, of which 7 done. ~10% complete.

DISCIPLINE (from CLAUDE.md, non-negotiable):
  - Docker-only execution: docker compose run --rm test/tools <args>
  - ONE TASK = ONE COMMIT
  - TDD throughout: RED test → GREEN impl → commit
  - Conventional Commits format
  - Never skip hooks, never destructive git, never edit interactively
  - Never weaken tests/integration/test_settings_repo_set.py without a spec
    amendment — those 9 tests are the safety net

PRE-EXISTING SURFACES TO REMEMBER (don't re-flag every session):
  - docker compose's "cli" service entrypoint is broken (myapp not in PATH);
    use "tools" service instead
  - Binance API key not authorized from local Docker IP; live equity
    introspection limited to VPS
  - Spec §A1 amendment: strategy_orders.order_id is UUID FK to orders.order_id
    (not BIGINT REFERENCES orders.id as the spec originally said)

ASK BEFORE CODING (one batch):
  1. Bring docker compose up if it's down? (yes/wait)
  2. Push the 15 commits to origin first, or keep local through more work?
  3. Begin AM-4e immediately, or different task ordering?

Then proceed in order: AM-4e → AM-4f → AM-5 → AM-6 → AM-7 → Phase 3 Task 1.5
onward. Stop at the end of AM-7 for operator review (effective end of the
amendment work). Stop again at end of Wave 0 (Phase 3 plan checkpoint 1).
```

---

## What this prompt assumes is true at session start

| Check | Expected |
|---|---|
| `git status` | Clean tree, branch `phase-3-strategy-pivot`, no untracked Phase 3 files |
| `git log -1 --format=%H` | `e251937...` |
| Untracked files | `.claude/`, `scripts/probe_*.sh`, `scripts/add_binance_mcp.sh`, `scripts/inspect_claude_config.py` (all pre-existing) |
| `docker compose run --rm tools alembic current` | head `0017` (when infra is up) |
| `tests/integration/test_settings_repo_set.py` | 9/9 passing |
| `policy.yaml` | `trading_enabled: false`, equity comment `~$183 USDT` |
| `runtime/CLAUDE.md` | unchanged from before this session (still heartbeat-trader persona) |
| Live capital | ~$183 USDT (operator-stated; not verifiable from local Docker due to Binance IP allowlist) |

If any check fails, the next session should stop and ask before proceeding.
